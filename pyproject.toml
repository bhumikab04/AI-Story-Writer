[project]
name = "ai-story-writer"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "langchain-google-genai>=2.0.0",
    "python-dotenv>=1.0.0",
]

[tool.vercel]
entrypoint = "app:app"
